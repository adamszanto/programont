spring:
  jackson:
    deserialization:
      fail-on-unknown-properties: true
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/doctors
    username: testuser
    password : ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver